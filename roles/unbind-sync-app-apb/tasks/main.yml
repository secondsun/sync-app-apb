---
# Delete client secret
- name: Delete {{ _apb_bind_creds.CLIENT_ID }}-sync-secret
  k8s_v1_secret:
    name: "sync-app-{{  _apb_bind_creds.CLIENT_ID | lower }}"
    namespace: "{{ namespace }}"
    state: absent
